# TMDB API Key Setup

To enable real TMDB data in strm, create a `.env.local` file in the project root:

```
TMDB_API_KEY=your_tmdb_api_key_here
```

Get a free API key at: https://www.themoviedb.org/settings/api

Without this key, the app runs on rich mock data automatically.
